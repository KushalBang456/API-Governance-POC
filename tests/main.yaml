openapi: 3.0.1
info:
  title: Main Branch Spec
  version: 1.1.0
paths:
  # 1. Legacy (Inherited)
  /legacy/users:
    get:
      summary: Ancient User Endpoint
      responses:
        '200':
          description: OK
  
  # 2. Modern (Currently Valid)
  /modern/orders:
    get:
      summary: Recent Orders
      parameters:
        - name: orderId
          in: query
          description: The ID of the order
          schema:
            type: integer # <--- NOTE: It is an Integer here
      responses:
        '200':
          description: OK