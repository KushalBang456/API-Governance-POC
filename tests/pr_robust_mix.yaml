openapi: 3.0.1
info:
  title: Robust Mix Test
  version: 1.2.0
paths:
  # 1. LEGACY (Modified)
  # Change: Added a POST method (Guaranteed Diff)
  # Violation: No description.
  # Expected: Python should IGNORE this (It's in Baseline).
  /legacy/users:
    get:
      summary: Ancient User Endpoint
      responses:
        '200':
          description: OK
    post: # <--- NEW METHOD ADDED
      summary: Legacy Update
      # No description
      responses:
        '200':
          description: OK

  # 2. MODERN (Modified)
  # Change: Added a POST method (Guaranteed Diff)
  # Violation: No description.
  # Expected: Python should INCLUDE this. Strict Lint should FAIL.
  /modern/orders:
    get:
      summary: Recent Orders
      description: Returns recent orders.
      responses:
        '200':
          description: OK
    post: # <--- NEW METHOD ADDED
      summary: Create Order
      # No description! (Violation)
      responses:
        '200':
          description: OK

  # 3. NEW BROKEN (Added)
  /new/broken:
    post:
      summary: I am new and broken
      # No description
      responses:
        '200':
          description: OK

  # 4. NEW VALID (Added & Complex)
  /new/deep-ref:
    post:
      summary: Deep Ref Test
      description: I am valid and complex.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Root'
      responses:
        '200':
          description: OK

components:
  schemas:
    Root:
      type: object
      properties:
        child:
          $ref: '#/components/schemas/Leaf'
    Leaf:
      type: string